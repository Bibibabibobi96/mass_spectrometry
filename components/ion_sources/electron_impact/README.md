# 电子轰击离子源

正式源脚本位于`comsol/`，通过[`ei_source_paths.m`](ei_source_paths.m)把新生成的试验 MPH
写入组件 artifact 的`scratch/comsol/`，结果写入`results/comsol/`。迁移前位于通用模型
目录的三个`MS_Stage1_EISource_*`模型已归入本组件`models/comsol/archive/`。
