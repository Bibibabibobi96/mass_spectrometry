# RF 四极杆碰撞冷却离子导向器

正式源脚本位于`comsol/`，通过[`rf_quadrupole_paths.m`](rf_quadrupole_paths.m)把新生成的
试验 MPH 写入组件 artifact 的`scratch/comsol/`，结果写入`results/comsol/`。历史
`test3`模型保存在`models/comsol/archive/`，尚未提升为正式基线。
