; Same minimal basis-count probe with the reflectron fractional-surface mode.
# local var = _G.var or {}
# local electrode_count = var.electrode_count or 21
# local nx = 2*electrode_count+3
pa_define($(nx),16,1,cylindrical,y,electrostatic,,1,1,1,surface=fractional)

# for electrode=1,electrode_count do
e($(electrode)) { fill { within { box($(2*electrode),2,$(2*electrode),13) } } }
# end
