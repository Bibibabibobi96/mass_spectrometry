# RF 四极杆项目状态

## 当前结论

`transport_no_collision + official_fixed_25.ion`回归已在 COMSOL 6.4 与 SIMION 2020 通过，并完成
GUI资产复验。它证明的是RF-only径向约束和轴向传输，不是碰撞冷却或质量过滤，也还不是需要
SolidWorks同步的机械正式模型。面向部件集成的N=100接口候选已完成真实独立场比较，但未达到预设
相空间一致性目标，因此当前状态是“回归有效、接口尚未闭合”，不得笼统称为双求解器正式闭合。
2026-07-18 单一运行配置、IOB运行时结构、候选SHA与manifest链已用SIMION固定25粒子复验：
IOB加载本地PA，单实例变换和`39×39×477 @ 0.2 mm`通过，25/25命中、传输率1.0；候选清单
14/14哈希通过，manifest状态为success且保持candidate身份。
旧碰撞脚本现仅保留拒绝执行短桩；其150 mm几何、硬编码LiveLink和未验证碰撞参数不属于当前契约。

## 系统架构与资格边界

项目链路固定为：人工维护的`baseline + particle source + mode + interface`，经解析器生成只读发布，
COMSOL与SIMION分别求场和推进，再以统一事件表、manifest和跨求解器比较闭合。任何GEM、Lua、MATLAB
脚本、MPH、PA或结果CSV都不是参数反写入口。`interface_contract.json`同时是事件表列名、枚举、参考平面
和比较语义的唯一机器权威，校验器运行时读取它，不另存一份常量。

`transport_no_collision`是不可变N25官方回归；`transport_interface_readiness`是在同一RF-only基础物理上的
接口资格叠加，不是第二套硬件。两者的run目录、结果文件和跨求解器报告按mode隔离。接口运行必须显式
指定粒子表、至少100个粒子和RF峰值；项目级Candidate门禁也必须显式选择mode，防止把N25回归产物
误当作N100接口证据。

Static只校验配置派生、生成资产与纯分析代码；Candidate追加指定mode的两端manifest、事件契约和功能
比较；Formal再追加机械正式几何和SolidWorks同步。当前Static可用，Candidate可执行且允许形成可追溯的
FAIL，Formal因尚未选定机械正式几何而有意拒绝。当前仍在演化的调查保留在本文件；只有结论已冻结且
正文影响阅读时才创建`docs/history/`快照，不创建空目录或空文档。

已闭合的运行器生命周期改进是：所有mode先完成粒子表、N和显式RF参数预检，再创建run/candidate目录，
因此输入拒绝不会留下无状态空壳。尚未闭合的实现项是求解器启动后的异常退出仍未统一补写
`failed/interrupted` manifest；这需要给两端生产入口增加共同的异常收尾协议，属于后续小型软件任务，
不影响现有success manifest和本轮静态门禁，但在它完成前不能把异常运行目录误认成可比较产物。

2026-07-18 起，已闭合的官方回归和面向未来集成的验证分为两条互不覆盖的链：
`transport_no_collision + official_fixed_25.ion`继续作为不可变的100 amu/N25回归；
`transport_interface_readiness`使用接口叠加解析契约、命名工况和粒子表，当前仍是尚未双求解器闭合的候选。
本阶段不连接oa-TOF、不修改其正式资产，也不引入碰撞或质量过滤物理。

同日新增的统一相空间链已由独立COMSOL/SIMION场实际复验。两端各25个粒子均产生100条合法事件，
source与权威ION表逐值一致，杆端85.4 mm和交接面90.2 mm坐标严格一致；两份运行manifest重新计算
全部输入/输出SHA后PASS。COMSOL候选MPH重开并经GUI Compute复算仍为25/25。

统一参数分为共享硬件、共享粒子源和功能模式三层：`../config/baseline.json`、
`../config/official_particle_source.json`、`../config/modes/*.json`；程序统一读取自动生成的
`../config/resolved_geometry.json`，全项目门禁为`../verify_project.ps1`。以后在集成仪器中，传输四极杆与质量
过滤四极杆实例化同一硬件模板，分别绑定 `transport_no_collision` 与 `mass_filter_reference` 配置和空间
变换；不复制两套近似几何。质量过滤配置目前只冻结官方参考值，尚未验证。

2026-07-20新增项目内求解器无关 L0 参考实现`../analysis/quadrupole_l0.py`，按根多极杆理论计算
Mathieu参数、第一稳定区尖点、RF-only截止、固定`U/V`通带和质量尺度，并由Static门禁运行理论向量。
审计确认SIMION官方例程的`22.7630149397 V`是单杆组相对`-8 V`公共偏置的DC幅值`U`，不是两组间
差值；机器字段已改为`dc_amplitude_V_per_group`，对应组间DC差值为`45.5260298794 V`。同一合同下
100 Th处`q=0.7060233010`、`a=0.2298878277`，扫描线`U/V=0.162804703`的理想稳定区
`q_in=0.682729174`、`q_out=0.710802209`、`R_stab=24.8197`。这些只闭合L0公式和电压语义，未运行
质量扫描、未验证质量峰/透过率，也不改变该mode的`frozen_future_mode_not_yet_validated`状态。

参数发布执行单向链：人工只修改`baseline.json`、粒子源、mode和接口契约；
`resolve_contract.py`生成解析发布，MATLAB通过`load_rf_quadrupole_contract.m`读取并检查派生关系，
`sync_simion_geometry.py`从同一解析发布生成两份GEM。SIMION安装目录的官方例程只作为几何来源证据，
不再参与运行时读取。Lua、GEM、COMSOL脚本和测试入口不得各自维护同一几何或运行参数。

`../config/execution_profiles.json`现将两种传输mode映射到Static、COMSOL、SIMION、跨求解器比较和
Candidate门禁的真实组合链。无碰撞profile只能消费现有入口已暴露的RF幅值与频率；接口profile还要求
显式粒子表和RF峰值；两者还显式声明当前分析能够评价的传输、TOF、束流半径/发散和平均能量目标。
其余已设想但入口尚未消费的变量或目标必须由dry-run报告`NEEDS_IMPLEMENTATION`，
profile存在不等于碰撞、质量过滤或正式机械设计已经完成。

求解器无关接口由`../config/interface_contract.json`定义。`z=85.4 mm`是杆端边缘场诊断面，
`z=90.2 mm`是出口孔口处的部件交接面，`z=95.2 mm`仍是独立部件验收检测器而不是集成边界。
两端必须在相同物理平面插值得到位置、速度、能量、发散角和RF相位，并保留所有粒子的终止原因；
不得拿求解器的电极表面回退坐标代替接口状态。

## 权威基线

- 几何：SIMION 2020 `examples/quad/quad_monolithic.gem`；总长 95.2 mm、杆段 79.6 mm、
  `r0=4 mm`、圆杆半径 4.592 mm、PA 单元 0.2 mm、入口孔半径 1.2 mm、出口/检测器半径 3.6 mm。
- 粒子：固定 25 个 100 amu、+1 离子；birth 0--0.909091 us、横向位置 ±0.05 mm、
  1.8--2.2 eV、绕 workbench +x 的填充 5° 圆锥，源表为 `../config/particles/official_fixed_25.ion`。
- IOB 实测位置坐标：PA x→workbench z、PA y→workbench −y、PA z→workbench x。Fly2
  `standard_beam` 的 `az/el` 在 IOB 放置前按其局部束流基向量解释；逐轨迹斜率审计表明，若
  `vSim=(v cos(el)cos(az), v cos(el)sin(az), v sin(el))`，则 COMSOL 速度必须为
  `(-vSim_y,-vSim_z,vSim_x)`，而不能机械套用位置的逆变换。
- 传输波形：两组对置杆 `±139.81792 V peak`、1.1 MHz、0 V DC/轴偏置；入口、出口、检测器均 0 V；
  无碰撞、无阻尼、无背景气体，Mathieu `q=0.7060233`。
- 收敛数值：COMSOL mesh auto level 1、80 RF 步/周期；SIMION quality 10、40 RF 步/周期。

## 验证结果（2026-07-17）

| 求解器 | N | 命中 | 传输率 | 平均检测时间 (us) | 最大杆区半径 (mm) |
|---|---:|---:|---:|---:|---:|
| COMSOL | 25 | 25 | 1.00 | 50.1155 | 0.5414 |
| SIMION | 25 | 25 | 1.00 | 49.7386 | 0.4729 |

2026-07-18在`z=90.2 mm`交接面的新增相空间诊断如下。N=25低于接口正式统计要求N=100，故只用于
官方回归与发现问题，不把候选目标自动转为正式门禁。

| 指标 | COMSOL | SIMION | 相对/绝对差 |
|---|---:|---:|---:|
| 交接率 | 1.000 | 1.000 | 0 |
| 平均TOF (us) | 47.1173 | 46.9399 | 0.377% |
| RMS束斑半径 (mm) | 0.43648 | 0.39166 | 10.824% |
| RMS发散角 (deg) | 5.9400 | 5.9460 | 0.102% |
| 平均动能 (eV) | 2.03253 | 1.99326 | 1.951% |

旧传输/TOF/约束回归PASS；候选接口目标中束斑半径10.824%略高于暂定10%，其余指标通过。
不得调参掩盖该结果。

同日已用固定seed `20260816`生成同源100 amu/N=100粒子表，并在两端各自求场、各自推进。
COMSOL与SIMION均为100/100传输，事件表各400行，source身份、四类事件平面、运行manifest及COMSOL
MPH重开后的GUI Compute全部PASS；正式接口统计门禁据此启用。结果如下：

| 指标 | COMSOL | SIMION | 相对/绝对差 | 暂定目标 |
|---|---:|---:|---:|---:|
| 交接率 | 1.000 | 1.000 | 0 | PASS |
| 平均TOF (us) | 47.3608 | 46.8626 | 1.057% | PASS |
| RMS束斑半径 (mm) | 0.48370 | 0.35993 | 29.343% | FAIL |
| RMS发散角 (deg) | 6.43944 | 4.93210 | 26.511% | FAIL |
| 平均动能 (eV) | 1.94555 | 1.99973 | 2.747% | FAIL |

因此N=25的束斑接近不是稳定闭合证据；N=100说明出口相空间差异具有系统性。回归门禁中的粒子身份、
传输、平均TOF和杆区约束均PASS，但候选接口目标整体FAIL。下一步应对相同N=100数据按入口、杆内、
出口边缘三个区段定位差异，并分别做两端空间离散收敛；不得调RF幅值、相位或粒子源追平结果。

本轮还发现SIMION Lua不存在`segment.load()`生命周期回调。此前官方N=25运行恰好与Lua默认值一致，
所以物理结果未因该错误改变；但显式运行配置实际上未被加载。现已改为程序加载时读取配置，并在赋值后
计算RF角频率和相位。Lua中的非零物理默认值已改为零占位和正值断言；随后重新构建、refine并运行
官方N=25，25/25命中且100条事件和manifest均PASS，证明当前结果来自显式配置。以后新增可调参数
必须用非默认工况证明配置确实生效，不能只靠默认工况冒烟测试。

传输率绝对差为 0，平均 TOF 相对差为 0.755%，两端最大杆区半径均远小于 `r0=4 mm`；跨求解器
门禁 PASS。速度映射修正前的所有 COMSOL 收敛扫描、候选 MPH、CSV、图像和报告均已删除，不能再
作为收敛证据。修正后 mesh1 的 80→160 RF 步/周期复核均为 25/25，平均 TOF 变化 0.058%、最大
杆区半径变化 0.47%，故 80 步/周期的时间积分已重新确认。空间复核显示较粗 mesh2 相对 mesh1
的平均 TOF 变化 0.98%、最大杆区半径变化 4.26%；显式更细的 `hmax=0.5 mm` 候选相对 mesh1 的
平均 TOF 变化 0.356%、最大杆区半径变化 13.4%。因此 mesh2 不可作为替代，mesh1 的渐近空间收敛
仍未闭合；不得依据与 SIMION 的接近程度选择网格。SIMION 的几何、PA 与固定粒子源未受此速度映射错误影响。

同一公共真空格点上的 COMSOL 自身场比较表明，mesh1 与显式 `hmax=0.5 mm` 的独立 FEM 单位场在
207706 个公共点的矢量相对 RMS 差为 0.446%，杆区 175959 点为 0.271%。因此 mesh1 的杆区电场已
接近更细网格；端点轨迹的更大差异应作为 RF 相位累积敏感性处理，不能直接视作场未收敛。

两端分辨率加密给出更严格的区域结论：SIMION 0.2→0.1 mm PA 的入口/杆区/出口自身场差为
3.396%/0.0467%/0.280%，COMSOL mesh1→`hmax=0.5 mm` 为 1.945%/0.271%/3.239%。细 FEM 与
0.2 mm PA 的杆区/出口差为 0.0521%/0.384%，与 0.1 mm PA 为 0.0289%/0.359%；杆区和出口随
加密闭合。入口的细网格交叉差仍为 4.466%，且两端自身都敏感，故不能把入口误差只归因于官方
0.2 mm PA。该测试只用于误差定位，不把细候选提升为生产基线。

对 SIMION PA 与 COMSOL FEM 的分量比较，杆区 `Ex/Ey/Ez` RMS 差分别为 32.58、30.19、35.43 V/m；
在杆中点 `z=45.6 mm,y=0` 的 `Ex` 横向梯度分别为 −12532.07 与 −12535.72 V/m/mm，相对差仅
0.0291%。这直接闭合了近轴四极杆聚焦梯度；现有轨迹末端差异不能归因于该局部场强度。

COMSOL MPH 重开后，`std1`/`std2` 通过 Study GUI Compute 路径复算仍为 25/25，Solver 标签保持
`sol1,sol2`。SIMION IOB 重开确认单实例、变换 `(az=-90, el=0, rt=180)`、PA 为
`39×39×477`、单元 `0.2 mm`，项目本地 Lua/Fly2 生效。

终点诊断图由 `analysis/plot_terminal_distribution.py` 生成到
迁移快照中的`legacy-layout/results/cross_solver/transport_no_collision_terminal_distribution.png`：上排两栏使用同一
PA/COMSOL 横向坐标与颜色尺度，虚线圈表示 3.6 mm 探测器口径，圆点是探测器命中、叉号是非探测器
终止，颜色表示终止轴向位置；下排按相同粒子 ID 给出 SIMION→COMSOL 终点差矢量和逐粒子横向差。
当前两端均为 25 个命中；未来撞杆、出口壳体或超时离子会保留在同一图中。

轴向诊断由 `analysis/plot_transport_trajectory_diagnostics.py` 读取两端统一 PA/COMSOL 坐标下的逐粒子
轨迹 CSV，输出 `transport_no_collision_r_vs_z.png`、`transport_no_collision_delta_r_vs_z.png` 和
`transport_no_collision_delta_time_vs_z.png` 和 `transport_no_collision_key_plane_distributions.png`。
它们分别显示 25 条 `r(z)` 包络、同 ID 的 `|r_COMSOL-r_SIMION|`（均值/P95）、同 z 的时间差、
以及入口 0.2 mm、杆中点 45.6 mm、杆出口 85.4 mm、检测器前 94.8 mm 的并列束斑。经速度映射
斜率修正后，入口横向距离均值从 0.0166 mm 降至 `8.6e-8 mm`；当前最大均值/P95 径向差为
0.3455/0.6568 mm，终端均值距离为 0.9698 mm。COMSOL 160 步与 SIMION 80 步的独立候选给出
0.3370/0.6492 mm 和 0.9784 mm，未实质降低差异；这排除了当前时间步长作为主因。两端在同一
`e1=+100 V,e2=-100 V` 单位场、同一真空采样格点上各自独立求场后，全部 208050 点的矢量相对 RMS
差为 0.515%，杆区 175959 点为 0.266%（`unit_rf_field_comparison.json`）。该比较没有把 PA 场导入
COMSOL 或把 FEM 场导入 SIMION，证明两场在杆区已数值闭合；剩余逐粒子轨迹终点差仍需作为 RF 相位敏感
的轨迹积分问题独立收敛和解释。诊断用于定位局部求解器差异，不替代传输率、TOF 和约束门禁。

`analysis/plot_transport_phase_diagnostics.py` 进一步在相同轴向位置比较到达时间、RF 相位和同 ID 横向
距离。入口处平均时间差为 `-8.9e-9 us`、平均横向距离 `8.5e-8 mm`，确认粒子释放仍对齐；杆中点的
平均时间差为 `91.5 ns`（平均包裹相位差 `36.2°`），杆出口为 `186.9 ns`，说明两端的轴向运动会逐步
产生 RF 相位漂移。但杆区 9975 个配对样本中，绝对包裹相位差与横向距离的 Pearson 相关仅 `r=0.095`，
故不能把横向差归结为一个可调的全局 RF 相位偏移；该图仅作诊断，未改写任一求解器的场或轨迹。

## 轨迹差原因审计

| 候选原因 | 检验结果 | 判定 |
|---|---|---|
| 初始位置、速度、出生时间 | 首个公共面横向均值差 `8.5e-8 mm`、时间差 `-8.9e-9 us`；出生覆盖同一 RF 周期 | 排除 |
| RF 幅值、频率、符号、相位零点 | 两端均为 `sin(2πft)`、±139.81792 V peak、1.1 MHz；单位场符号和中点梯度一致 | 排除 |
| 入口/出口边缘场 | 基线入口/杆区/出口场差 3.46%/0.266%/3.22%；双端加密后杆区/出口降至 0.0289%/0.359%，入口仍 4.466% | 主要证据支持 |
| 时间积分步长 | COMSOL 80→160 步的 TOF/杆区半径变化 0.058%/0.47%；SIMION 40→80 为 `1.05e-6`/0.030% | 排除主因 |
| 单一全局 RF 相位偏移 | 杆区相位差与横向距离相关仅 `r=0.095` | 排除 |
| 探测面或终止插值 | 横向均值差在出口壳体前/探测器前已达 0.506/0.883 mm，比较面为 0.970 mm | 排除主因 |
| 推进器算法差异 | 两端各自时间加密均稳定；z=20 mm 杆内释放至 z=70 mm 的平均横向差仅 0.000990 mm | 排除杆内主因 |

基线横向差从入口边缘开始：均值在 `z=5.0 mm` 首次超过 0.01 mm，杆入口已为 0.0305 mm，随后在
杆内随 RF 相位漂移累积，并在出口边缘进一步放大。为隔离入口，将同一粒子源仅沿轴向平移到
`z=20 mm`，两端仍用各自独立场和推进器：首个公共面 `z=20.2 mm` 的均值差为 0.000096 mm，杆中点
0.000642 mm，`z=70 mm` 仅 0.000990 mm；至杆出口才升为 0.02764 mm，预探测器处为 0.4355 mm，
0.01 mm 阈值首次出现在 `z=84.4 mm`。这直接证明杆内场和积分高度一致，基线差主要由入口边缘注入，
而内部释放后的剩余差主要在出口边缘放大。边缘差来自两端边界离散表示，不能只归罪于 0.2 mm PA；
该定位不要求、也不允许共享求解器场。

## 后续功能的闭合判据

按用户 2026-07-17 的决定，跨求解器的同编号单粒子轨迹或落点高度重合不作为通用硬门禁；边缘场仍须
按其对目标功能的影响验证，不能因当前传输通过而永久忽略，也不得为追平另一求解器而调参或选网格。

- 无碰撞传输以传输率、TOF、杆区约束、出口束斑/角度/能量和接口接受率为主；仅当孔径、下游接受率
  或损失位置对端部敏感时，继续做边缘场局部加密与结构复核。
- 碰撞冷却采用足够样本量的统计闭合，比较传输率、冷却时间、出口能量与径向分布、停留时间和碰撞
  统计；随机碰撞后不要求逐粒子对应。
- 质量过滤必须重新验证入口捕获和出口释放的边缘场影响，以质量扫描峰位、峰宽/分辨率、透过率、
  稳定区边界及相位/初始能量敏感度闭合为准，不以探测器单粒子落点一致代替质量响应闭合。
- 多部件集成以接口相空间和下游功能接受率验收。若功能指标已收敛，单粒子落点差只作为诊断量；若
  功能指标对边缘离散仍敏感，则必须先完成相称的空间网格/PA分辨率收敛，才能提升为对应功能基线。

## 下一步

四极杆质量过滤、六极杆、八极杆等未来独立设计线只由根
[`docs/ROADMAP.md`](../../../docs/ROADMAP.md)规划；本节只保留当前四极杆碰撞冷却与传输项目的开放事项。

RF→oa接口按`../config/rf_to_oatof_interface_stages.json`顺序实施，当前进入S1，不允许直接跳到带透镜或
完整脉冲的复杂模型：

| 阶段 | 最小新增内容 | 样本与门禁 | 允许结论 |
|---|---|---|---|
| S0 数据直连参考 | 零长度虚拟边界；只做坐标、时间、身份和事件映射 | 复用两份RF N=100，不运行求解器 | 仅证明数据链；不表示物理连接 |
| S1 共面开孔无oa脉冲参考 | oa屏蔽参数化开孔；RF出口与入口理想共面；无独立连接器、间隙、管或透镜 | 孔径—接受率、RF时变场+oa常开静电场的联合模型及N=100功能门禁 | 最简单物理拓扑参考，不证明oa脉冲或公差 |
| S2 被动接地连接器 | 只增加真实有限间距和接地管/外壳 | 三维RF/静电边缘场、泄漏、损失和两端离散收敛 | 被动输运，无oa脉冲 |
| S3 脉冲捕获 | 在S2几何上增加最小波形、触发和三维捕获体积 | 全局时钟、脉冲/步长收敛及N=100捕获 | 名义装配下的动态捕获 |
| S4 主动光学与公差 | 仅当被动方案不能满足冻结指标时增加最少转向/聚焦电极及误差变量 | 电压、位姿和交互矩阵 | 有界候选工作区，不自动转正 |
| S5 跨软件闭合 | 冻结最简单通过方案并同步COMSOL、SIMION、SolidWorks | N=100先行，PASS后N=1000及完整Formal门禁 | 明确晋升后才能称为已连接 |

“没有连接器”只在两种受控含义下成立：S0没有物理接口资格；S1可以没有**独立连接器部件**，但仍必须
切开真实oa屏蔽、定义共面真空边界、电位参考和联合场。任何阶段都不能把粒子从RF出口直接平移进现有
封闭oa几何。某阶段FAIL时保留证据，只增加下一阶段所需的最小物理，不回调上游粒子源、不突破理论
孔径上限，也不修改当前Formal资产。

S0已由求解器无关run `20260721_084441__analysis__python__rf-oatof-s0__n100`完成。它复用既有RF-COMSOL
和RF-SIMION两份N=100出口证据，分别得到100个虚拟入口事件，均为零丢失；目标面最大残差和三类时钟
最大残差均为0。目标法向最小速度分别为`1656.466`与`1853.181 m/s`，虚拟入口RMS横向偏移分别为
`0.483701`与`0.359928 mm`，只保留为上游束团差异诊断。输出明确记录`physical_link=false`、
`field_transport=false`、`aperture_clipping_applied=false`，因此S0 PASS只关闭数据链，不改变oa屏蔽仍封闭
这一事实。S1已建立参数化开孔和局部联合场特征化原型；RF组件屏蔽审计发现杆区没有连续接地侧壁后，
旧外部真空盒实现已被失败关闭，并以连续接地圆柱壳的内部真空局部模型取代。联合场与N=100动态功能
链现已运行，但仍不等于S1性能或数值资格通过。最低通过率在特征化阶段保持未定义，只有
宣称S1 PASS或晋升前才必须由整机需求冻结。

S1-A求解器无关预检run `20260721_085453__analysis__python__rf-oatof-s1-aperture__n100`进一步修正了
“束流包络是硬几何下限”的过强假设。孔径理论上限只约束可开尺寸；超出孔径的束流尾部应记为入口损失，
是否可接受由预先冻结的最低通过率决定。在轴向完整高度严格小于耦合理论`1.0 mm`的极限下，RF-COMSOL
与RF-SIMION两份N=100入口事件的最佳轴向几何接受分别为`85/100`和`96/100`；要求80%时所需完整高度
分别为`0.889558/0.700713 mm`，要求90%时则为`1.090776/0.845015 mm`。因此90%要求对COMSOL源束在
当前理论上限下不可达，但这不等于S1没有有限通过率解。上述只是单轴理想截取，三维孔和联合场只会
进一步降低接受率。按用户2026-07-21决定，首个特征化候选不冻结最低通过率：轴向完整孔高固定为
理论上限的90%，即`0.9 mm`（半高`0.45 mm`）。RF出口物理半径`3.6 mm`只给出横向无额外粒子收益
上限`7.2 mm`，不是最终孔宽；最终横向值还必须与加速器轴心场比较，由离轴场诊断、最终分辨率影响、
泄漏、机械和高压约束共同取交集。矩形孔中心仍为oa一级加速间隙中点，轴向至上下电极各保留
`1.05 mm`。允许生成`1.0/0.75/0.5/0.25 mm`受控特征化候选，但横向值未解析前禁止选择或晋升候选；
当前Formal屏蔽仍封闭，S1状态仍为BLOCKED。

闭合Formal场参考run `20260721_093712__analysis__comsol__accelerator-transverse-field__grid`在严格对称的
31个横向位置和98个轴向位置上导出3038个保存解样本，不重求解、不追踪粒子。它以当前Formal源宽
`±0.5 mm`范围内的最坏轴向场轮廓RMS、势轮廓RMS和横向场RMS建立三个L0诊断告警值；闭合屏蔽参考的
连续告警清除区为完整横向宽度`1.0 mm`，而`±0.75 mm`触发告警。这些数值来自闭合场本身，不是由
分辨率、束斑或损失反标的物理容差，因此不再拥有硬否决权。真实侧孔仍必须在同一RF/oa联合开孔几何中
重采样，但最终孔宽由数值可信的粒子性能与其他硬约束决定。

局部联合场历史诊断已经证明旧告警不适合承担数值收敛门禁：闭合对照的加速器局部`hmax=0.5→0.25 mm`
使边缘处轴向场轮廓指标约从`2.51243e-4`变为`3.23276e-4`，势轮廓指标从`8.62865e-6`变为
`1.28506e-5`并跨过告警线，而横向场指标仅从`6.66435e-4`变为`6.37466e-4`。oa-only闭合对照把
人工外部真空裕量从`1→10→30 mm`后，势轮廓指标又呈非单调变化。这些run现只保留为历史诊断；新候选
采用连续0 V屏蔽并只网格化内部真空，不再用外部真空裕量或350 mm飞行管截面选择连接器。`0.5 mm`
开孔候选在某一级旧网格清除告警不能视为已经收敛或已经选定。

2026-07-21屏蔽拓扑审计确认COMSOL与由同一合同生成的SIMION几何一致：两者只有入口孔板和
`z=89.4--95.2 mm`的短出口enclosure，`z=1.8--89.4 mm`杆区没有连续接地侧壁；当前`7.6 mm`还是
参考PA/短出口外边界，而杆的最外缘为`8.592+4.592=13.184 mm`，不能把`7.6 mm`直接延长为外壳。
`../config/rf_continuous_grounded_shield_candidate.json`因此冻结独立的同轴圆柱诊断扫描：内半径取杆最外缘
的`1.5/2/3`倍，即`19.776/26.368/39.552 mm`，未选择最终半径，电场模型只需0 V圆柱面而不冻结
机械壁厚。选择最小可用半径时必须同时通过中心区RF谐波、空间收敛、N=100传输、出口相空间、RF向oa
泄漏、oa向RF反泄漏及高压净距；尺寸不得由oa外壳大小反向决定。RF圆柱外壳属于RF组件，圆柱端孔到
oa侧孔之间任何有限长度、变径或位移补偿结构属于S2连接器；未建模馈通不得获得“完全屏蔽”声明。

二维杆中段屏蔽扫描已完成三个`hmax=0.2 mm`半径和最小半径的`hmax=0.1 mm`复核。在工作边缘
`r=3.6 mm`，`19.776→26.368 mm`只使主四极幅值相对变化`2.58e-8`，六极/十极归一量绝对变化约
`3.55e-8/2.19e-8`；最小半径`0.2→0.1 mm`使主四极幅值变化`3.17e-7`、场RMS变化`1.81e-5`。
全域0.1 mm运行约309 s，而同半径0.2 mm约47 s，故二维筛选保留0.2 mm，停止无收益的全域细化，
把`19.776/26.368 mm`带入后续三维边缘场和N=100验证。该降维结果不选择物理半径，也不证明入口、
出口、馈通、气室或RF→oa连接。

三维网格筛选已从全四面体局部加密转为“均匀杆区扫掠棱柱 + 两端四面体”的混合方案。均匀杆区采用
`r<=8 mm`的`0.2 mm`横向网格、外区`1.0 mm`和40层轴向扫掠；端区只比较`hmax=0.5/0.25 mm`。
两级完整内部真空网格分别为`1584535/4184759`个单元，均无problem标记。分段全器件扫掠在两次冻结
尝试中仍因拓扑变化面不兼容而失败，已关闭该路线；不再通过局部修补或第三套网格追逐数值极限。

同一R=19.776 mm、同一N=100粒子表、RF波形、80步/周期和90.199 mm评估面已经在两级混合网格中
完成配对功能诊断。两端均为`100/100`通过且分类零变化；粗网格相对细网格的出口RMS半径差约`0.514%`、
RMS发散差约`1.042%`、平均能量差约`0.050%`，逐粒子二维位置RMS差`0.0434 mm`。逐粒子到达时间
RMS差`0.05345 us`，对应RF相位RMS差`0.369 rad`、最大绝对差`1.058 rad`，可能影响未来脉冲捕获，
但不能由局部场或相位数字单独判为功能失败。停止`0.125 mm`加密，保留端区`0.5 mm`为低成本候选、
`0.25 mm`为参考。

两份出口状态随后在只读SIMION oa-TOF Formal静电分析器中完成E2投影。`0.5/0.25 mm`分别为
`96/100`和`97/100`命中，平均局部TOF相对差约`9.25e-7`，RMS落点半径相对差约`0.0567%`；传输差
正好为预先冻结的1个百分点数值预算。3个命中分类互换粒子的可检测落点均位于有效半径的`98.28%`
以外，属于40 mm硬边缘敏感区。两网格的`r99=39.605/39.626 mm`，均对36 mm预算给出相同FAIL，
所以网格没有改变本阶段功能决策，端区`0.5 mm`正式保留用于下一候选阶段，不补跑0.125 mm或昂贵的
COMSOL下游重复。该投影把RF截面刚体映射到oa内部参考释放中心，不包含真实侧孔或连接器；整个束团
仍未满足36 mm的最终`r99`预算，但按本阶段“功能先贯通”的目标接受96%传输，不再把该预算作为流程
阻塞项。本结论不选择屏蔽半径、不声明物理S1连接，也不允许Formal晋升。

同一低成本RF出口状态随后完成共享仪器时钟的有限提取脉冲测试。粒子按
`44.476569--50.862918 us`的RF出口时序依次投影到Formal封闭内壁内侧1 mm的等效入口面，并连续计算
脉冲前轨迹；脉冲前整个加速器电极组为`0 V`。唯一固定脉冲时刻按平均入口时刻加入口至源中心的
名义飞行时间派生为`54.452426 us`，宽度`1 us`，脉冲内切换到Formal的
`2240/1760/分压环/0 V`电压组，随后恢复0 V。run
`20260722_083000__sim__simion__rf-entry-finite-pulse__n100__r13`中`95/100`粒子实际到达脉冲，
`44/100`最终到达探测器；全程0 V无脉冲对照为`0/100`。因此当前选定
`RF-COMSOL 0.5 mm混合网格 → 等效入口投影 → 脉冲前飞行 → oaTOF-SIMION统一有限脉冲 →
脉冲后飞行 → 探测器`链路功能PASS。44%只证明流程贯通，不是优化后的传输率；真实入口开孔、
连接器场、有限边沿和N=1000分辨率回归转为后续精化，不阻塞当前功能集成。r13新增入口、脉冲和终态
三类稀疏事件及两张诊断图，总计295行而非密集轨迹；完整终态清点和脉冲六维状态门禁均PASS，动力学
结果与r12相同，故r13为当前N=100功能基线。

随后进入S1真实开孔的局部联合场。连续RF壳的`19.776 mm`内半径根据既有二维、三维和N=100混合网格
证据仅保留为S1候选场半径，不是机械或Formal尺寸；局部COMSOL模型只包含RF内部真空、连续0 V圆柱壳
与完整端板、oa加速器内部真空及侧壁开孔，不含外部真空、350 mm飞行管、反射器或探测器。首次开孔
运行曾因域级Box选择把`0.25 mm`局部hmax误施加到整个连通真空而超过10分钟并占用约12 GB，已保留为
interrupted证据；改为只选择4 mm端口真空域后，同一`1.0×0.9 mm`案例约72 s完成。

闭孔配对run为`20260722_090000__analysis__comsol__rf-oatof-s1-joint-field__closed-rf-oa-h1-buf5__r02`，
开孔run为`20260722_092000__analysis__comsol__rf-oatof-s1-joint-field__w1-h1-buf5__r02`，增强后处理run为
`20260722_094000__analysis__python__rf-oatof-s1-joint-field-reanalysis__w1__r02`。开孔相对同域、同网格
闭孔的轴心Ez/电势相对RMS变化为`5.5640e-4/1.7294e-5`；旧闭壳均匀性告警未全部清除，但它不是硬
性能门禁。更关键的屏蔽诊断为：oa脉冲场向RF上游的最大泄漏仅`0.001681 V/m`，相对联合轴线oa场峰值
`3.814e-9`；实际RF峰值场在oa源中心附近仅`5.853e-15 V/m`，相对RF轴线峰值`4.315e-18`。因此不扫
更小孔，冻结`1.0×0.9 mm`为确定性N=100物理过孔候选并授权粒子候选；S1 PASS、分辨率和Formal晋升
仍未授权。

N=100物理过孔与统一脉冲功能链随后完成。COMSOL run
`20260722_103000__sim__comsol__rf-oatof-s1-physical-port-pulse__n100__r03`在真实侧孔外表面按每粒子的
`instrument_time`逐个释放，RF持续工作，oa电极只在`54.452426--55.452426 us`使用统一矩形提取脉冲；
100个RF出口粒子中88个落入`1.0×0.9 mm`孔，28个穿过加速器与下游5 mm局部边界。MATLAB把布尔列写成
`0/1`曾使首次后处理误报FAIL；不重跑求解器的分析run
`20260722_110000__analysis__python__rf-oatof-s1-physical-port-reanalysis__n100__r01`修正后为PASS。

这28个局部出口状态随后以恒等坐标变换送入只读oaTOF SIMION Formal分析器：保持真实三维位置、速度、
粒子ID、年龄和绝对出生时间，不再投影到源中心；同一脉冲按共享时钟继续到其真实结束。运行中发现并
修正了既有ION角度合同错误：加速器instance 3把PA局部速度映射为工作台`(vx,vy,vz)=(vx,vz,-vy)`，
因此必须先逆变换速度再生成azimuth/elevation。最终run
`20260722_140000__sim__cross__rf-oatof-s1-physical-end-to-end__n100-r07`得到阶段清点
`100 → 88 → 28 → 9`，即9%端到端命中、32.14%局部出口后命中；228行稀疏事件表和阶段漏斗图均进入
manifest，不保存密集轨迹。这证明真实开孔、局部联合场、逐粒子进入/统一脉冲和下游分析器的N=100功能链
贯通，但9%不是优化传输率，不作分辨率、S1/S2/S3资格、N=1000或Formal连接声明。r13等效入口投影run
因旧ION方向角合同只保留为历史流程证据，不再作为当前接口功能基线。

分析run `20260722_151500__analysis__python__rf-oatof-s1-loss-atlas__n100__r02`只读消费r07稀疏端点，生成六面板
坐标损失图集：入口孔`y-z`、COMSOL局部`x-z/y-z`终点、局部出口`x-y`最终命运、出口径向相空间和
SIMION终态`x-y`投影。它显示12个几何拒绝、60个局部停止、28个局部出口及9个检测命中，不使用或
伪造密集轨迹；图中的终点连线不得解释为真实轨迹。

入口相空间参考run `20260722_164500__analysis__python__rf-oatof-s1-entry-ideal__n100__r04`从oaTOF baseline与
Formal validation读取当前`1×1×1 mm³`源、`5±0.4 eV`能量合同和随机种子，只把质量匹配为本链路的
100 amu、+1，并与RF出口全部100个及物理孔内88个粒子比较。孔内RF粒子的平均能量/速度为
`1.9913 eV/1959.54 m/s`，理想参考为`5.0334 eV/3114.50 m/s`，平均能量与速度比例分别为
`0.3956/0.6292`；RF方向偏离`+x`的RMS为`4.521°`，理想参考为0。孔径筛选只使RF平均能量相对全部
出口粒子下降`0.00662 eV`，说明主要差异来自上游相空间而非12个几何裁剪。该比较发生在物理孔入口面
与理想释放体两个不同物理位置，只作诊断参考；它本身不得用于判定接受率或修改oa Formal源，等时刻
状态由下述脉冲前快照另行回答。

COMSOL重放run
`20260722_173000__sim__comsol__rf-oatof-s1-pulse-capture__n100__r01`现以同一冻结输入和脉冲额外导出
脉冲开启前的稀疏三维状态，每个仍存活粒子仅一行，不保存密集轨迹。物理孔内88个粒子在
`54.452426 us`时全部仍有有效状态，脉冲前动态损失为0；但它们沿注入`x`方向铺开约20 mm，进入当前
oa Formal `1×1×1 mm³`理想释放参考体积的只有3个。分析run
`20260722_181000__analysis__python__rf-oatof-s1-capture-ideal__n100__r01`进一步得到：88粒子脉冲前平均
能量/速度为`1.9914 eV/1959.59 m/s`、方向偏离`+x`的RMS为`4.519°`，质量匹配理想源仍为
`5.0334 eV/3114.50 m/s/0°`；入孔至脉冲的时间跨度为`3.590--9.976 us`。因此当前实现准确地完成
“按到达时刻进入同一加速器场并接受统一脉冲”，却没有让束团在脉冲前形成紧凑储存态；此前不得将其
简称为已完成“捕获--储存”。理想释放体积只作同位置尺度的诊断参考，不是硬孔径或物理接受度。下一步
应先选择功能定义：若只需连续束切片提取，则以脉冲延迟/宽度对探测命中和分辨率做低成本扫描；若必须
积累后统一提取，则需新增可真实约束注入方向运动的储存电场/电极及门控时序，不能仅调整现有脉冲时刻。

为先关闭2 eV实际RF束与5 eV oa Formal参考的均能差异，接口源族新增独立
`rf_to_oatof_100amu_5eV`工况；2 eV官方回归源保持不变。首次5 eV run
`20260722_193000__sim__comsol__rf-input5ev-handoff__n100__r01`虽得到正确均能，但固定能量生成分支少消耗
一组随机数，使后续方向不再与2 eV源逐粒子配对，故只保留为排错证据。生成器修正后，r02与2 eV控制
除入口动能外的出生时间、位置、方向、质量和电荷逐行完全相同。COMSOL run
`20260722_193000__sim__comsol__rf-input5ev-handoff__n100__r02`保持原几何、RF、0 V静态电极、无碰撞和
`hmax=0.5 mm`混合网格，得到100/100通过、出口均能`5.0059 eV`、能量样本标准差`0.0268 eV`，因此
5 eV入射在当前无碰撞RF中保持到交接面，均能匹配PASS。分析run
`20260722_201500__analysis__python__rf-input-energy-2ev-vs-5ev__n100__r01`同时显示飞行时间均值缩短
`17.433 us`，出口RMS半径从`0.3571`增至`0.4088 mm`（+14.5%），发散RMS从`4.713°`增至`6.455°`
（+37.0%）。后两项反映更快离子在同一RF参数下经历较少横向约束，不否定能量匹配，但必须送入同一
真实物理孔和oa脉冲模型复验；此阶段不修改Formal源，不优化RF，也不声明下游性能PASS。canonical
交接转换器现只在manifest模式、N=100、`hmax=0.5 mm`、命名工况、冻结能量合同SHA及其关键物理字段
全部一致时接受该5 eV来源；当前合同可增加运行证据而不使冻结来源失效，也不能通过伪装成旧2 eV模式
绕过来源资格。

随后保持旧功能基线相同的14 mm脉冲定时距离，仅按新束真实速度重算统一脉冲：
`29.862085 us + 14 mm / 3088.329 m/s = 34.395281 us`；同一公式可精确复现旧值`54.452426 us`。
COMSOL真实`1.0×0.9 mm`孔run
`20260722_210000__sim__comsol__rf-oatof-s1-physical-port-5ev__n100`得到`100→77→39`，再由SIMION
Formal分析器run `20260722_213000__sim__cross__rf-oatof-s1-physical-5ev__n100`得到37个检测命中，
下游39粒子中37个命中。与旧2 eV功能基线`100→88→28→9`相比，5 eV束因发散增大而多损失11个入口
粒子，但更高轴向速度使局部出口和最终命中分别增加11和28个；因此能量变化对不同阶段的影响相反，
必须保留分阶段漏斗，不能只报总通过率。脉冲快照比较run
`20260722_214500__analysis__python__rf-oatof-s1-capture-ideal-5ev__n100`显示77个入孔粒子均活到脉冲、
均能`5.0038 eV`，与质量匹配oa理想源只差`-0.0296 eV`，但理想参考体积内仍为0个。该候选因此通过
N=100功能贯通，不证明紧凑储存、分辨率、S1晋升或Formal集成。

场到性能实验由`../config/rf_to_oatof_field_performance_experiment.json`冻结为E0--E4证据链。仍只有当前
耦合加速器--反射器理论对应的一个Formal baseline；同粒子状态重放只是诊断参考。`z`是oa加速/纵向
聚焦方向，`x`虽横于oa加速轴却是RF注入方向，`y`为侧向，故不再用单一`sqrt(Ex^2+Ey^2)`隐藏方向
差异。E1以最终性能预算而非旧场告警为数值尺度：最后两级设置造成的性能不确定度不得超过每项允许
损失的20%，且不得改变分类。RF混合网格已经触发“下游功能裁决而非继续局部加密”的停止规则，
`hmax=0.125 mm`不再授权。E2用N=100和共享全局时钟筛选，
E3只对最多两个候选用N=1000、有限波形oa脉冲及全分析器验证分辨率，E4再处理RF相位、脉冲延迟、位姿和
跨软件鲁棒性。暂定性能预算为分辨率不低于Formal的99%、探测器RMS束斑不超过Formal的105%、`r99`
不超过有效半径的90%，几何过孔后的附加场致损失不超过1个百分点，均按95%置信区间和逻辑AND判断。

名义电位参考已由现有机器权威闭合：RF出口enclosure、RF轴偏置和oa加速器屏蔽均为`0 V`。S1场特征化
的oa基场为静态，但动态功能扩展已经在同一开孔几何中加入有限提取脉冲；1.1 MHz RF始终保留。开孔后
的RF边缘场与oa电场统一归`rf_to_oatof_S1_joint_vacuum_and_electrode_model`所有，所有基场必须在相同三维开孔几何和
共同0 V参考上求解，并按全局仪器时间恢复RF相位。独立封闭部件的旧场不能拼接成联合场；当前只闭合
了所有权并建立候选局部联合场实现，但尚未取得开放端口的数值与粒子性能闭合。

2026-07-19已将上述定位、收敛、预算和集成决策固化为
`analysis/assess_interface_integration_gate.py`，只消费已有独立场结果，不重跑求解器。工具按固定顺序
输出`edge_localization.json`、兼容旧文件名但角色为`interface_discrepancy_assessment`的
`interface_error_budget.json`和`oatof_integration_gate.json`：

1. 边缘定位PASS：基线差异从入口过渡开始；绕过入口后杆内至z=70 mm仍接近，剩余差异在出口放大。
2. 接口差异评估FAIL：N=100接口的束斑、发散和能量仍超出既定目标。局部场RMS加密变化与相空间
   输出是不同且非线性耦合的观测量，禁止把百分比相减后宣称误差已覆盖；当前证据尚不构成含置信
   区间和传播规则的完整误差预算。
3. 集成门禁FAIL：原N25/N100部件回归仍PASS，但严格接口FAIL；RF→oa-TOF变换/时间合同已有候选运行
   证据，下游功能接受度也已实测为FAIL。因此不生成、不连接候选包。

门禁状态机现要求部件回归必须PASS，且缺少冻结集成合同时不能得到集成PASS。合同完整后，严格接口
PASS才给PASS；严格接口FAIL但外部提供的同一冻结下游模型功能验收PASS时，才允许
CONDITIONAL_PASS。回归FAIL、功能验收FAIL或证据未完成均禁止打包。当前复算仍为FAIL，blocker同时
包含`strict_interface_failed`和`missing_integration_contract`。

当前下一步只保留两条互斥闭合路径：若要求严格相空间一致，修改或加密入口/出口边缘表示后复跑同一
N=100门禁；若真正需求是下游功能，先冻结空间变换、接受度指标和容差，再将两端相空间独立送入同一
oa-TOF接受度模型。不得先连接后调参，也不为本轮方法验证继续堆高精度扫描。

2026-07-20已建立`../config/rf_to_oatof_handoff.json`和
`../analysis/build_oatof_handoff.py`作为上述第二条路径的静态前置，但合同保持
`draft_blocked_by_electrical_and_physical_interface`且`package_generation_allowed=false`。其坐标候选使用
右手刚体变换`RF +z→oa +x、RF +x→oa +y、RF +y→oa +z`，把90.2 mm交接面中心投影到当前oa-TOF
源中心；这只是相空间投影，不表示粒子已穿过真实间隙、孔径、注入透镜或电位势垒。

部件链时间不再定义为“RF时间加oa时间”的专用字段，而是由状态包携带共享`clock_epoch_id`、
`instrument_time_us`、`lineage_age_us`、`particle_age_us`和`last_component_elapsed_time_us`。全局时间从
仪器统一触发起算，谱系年龄从根源粒子出生起累计，当前粒子年龄则在反应生成子粒子时归零。任意上游
离子源或其他部件均按组件耗时递推；碰撞或反应生成新粒子时还须增加`parent_particle_id`与`generation`。
当前oa-TOF是静电模型，派生ION可以把求解器局部出生时间设为0，但逐粒子全局时间保留在
权威状态包和row map中，最终整机时间必须复原为`handoff instrument time + oa elapsed time`；未来脉冲
提取或其他时变场不得无条件归零，必须直接消费全局时间或施加严格等价的相位偏移。
当前RF运行在根粒子出生时开始且没有反应，因此其`elapsed_time_us`暂时同时表示谱系年龄、当前粒子年龄
和RF组件耗时；离子源等上游部件接入前，RF输入/输出合同必须接受入口的三类时钟，并另行输出RF自身
耗时，不能继续复用一个字段。多电荷态的求解器质量固定按
`mass_amu=(m/z)[Th]×|charge_state|`派生。

转换器已对归档的COMSOL和SIMION两份N=100 handoff数据只读复验：两端均100/100转换，源manifest
哈希匹配，坐标旋转行列式为1，最大速度—动能相对残差分别为`7.65e-15`和`4.39e-6`。这些结果只证明
求解器无关状态与派生格式可复现。

oa-TOF随后在隔离run `20260720_164843__sim__cross__rf-handoff-projection__n100`中实际消费两份bundle，
分别送入只读正式COMSOL静电解和正式SIMION IOB。统一按40 mm有效半径重判命中后，RF-COMSOL束团
在oa-TOF COMSOL/SIMION中为`70/100`与`68/100`，RF-SIMION束团为`99/100`与`100/100`；同一束团
的两个下游求解器只差1--2个百分点，但两份上游束团相差29--32个百分点。两端平均局部TOF相对差
均不超过`1.07e-5`，RMS落点相对差不超过`0.353%`，说明失败是严格RF出口差异在探测器口径边缘被
放大，而不是oa-TOF求解器互相矛盾。全局检测时间和两类年龄复原PASS，manifest覆盖26项输出并PASS。
该N=100结果不作分辨率声明；电气参考和真实间隙/注入光学仍未建模，故功能门禁为FAIL、物理连接
继续BLOCKED，禁止通过移动源、扩大探测器或分别调参追求PASS。oa-TOF已形成项目内实际消费者，待
物理接口闭合及通用性证明后再判断是否提升到`common/`。

2026-07-21新增`../config/rf_to_oatof_interface_candidate.json`和
`../analysis/build_interface_handoff.py`，把未来物理连接明确拆为RF出口面、连接器、oa入口面和统一
脉冲时刻的三维捕获状态。唯一canonical传输量是每粒子一次的首次正向过面事件：身份、物种引用、
全局时间、三维位置、三维速度及可选出生时间；质量、电荷、动能、半径、发散、RF相位、年龄和求解器
局部出生时间均由事件表与单份JSON派生，不逐行重复。现有详细`particle_state.csv`继续承担RF内部诊断，
旧静态oa投影合同继续承担已完成运行的兼容入口，两者都不被新候选冒充为物理接口。

双边界合同从当前RF契约读取`z=90.2 mm`出口面和`3.6 mm`出口孔径，但故意把oa入口几何、连接器场、
脉冲波形和捕获体积保留为未解析。当前oa-TOF的`1×1×1 mm³`只登记为Formal参考释放分布，
`hard_acceptance=false`；`40 mm`仍只是oa检测器有效半径，不是RF出口束斑或oa入口孔径。接口不得在RF
出口按1 mm预裁剪粒子，而应分别统计`source_exit_exported → target_entry_crossed → pulse_captured →
detector_hit`。接口场重叠时必须归同一个连接器或联合模型，不能靠增加粒子表列数弥补场所有权缺失。
静态门禁已验证双边界不混用、派生量不进入canonical CSV、全局到局部时钟映射以及按需场自由快照；
这些轻量测试不证明真实入口接受度或脉冲捕获性能，运行打包仍被禁止。
两份归档COMSOL/SIMION N=100源表还通过新入口的只读`--check-source`兼容检查，均识别100个首次正向
出口事件，最大速度—动能相对残差分别为`7.65e-15`和`4.39e-6`；检查没有写出重复事件表或三维快照。

接口现同时允许RF出口与oa入口存在名义平移和转角，并把装配误差单独列为六个平移/转动候选变量。
合同不直接维护一份可独立手改的RF→oa变换，而是先在统一`instrument_global`坐标中分别冻结两个组件
位姿，再唯一派生`target_from_source`刚体变换。参考实现验证`r_target=R r_source+d`、
`v_target=R v_source`、全局时间不变，并拒绝非正交或行列式不为+1的旋转。该变换只解决坐标注册；粒子
跨越相对位移所需的间隙、弯转、聚焦或校正场必须在连接器中真实计算，禁止在后处理里把出口粒子平移
到oa入口。名义位姿和装配容差尚未冻结，因此仍不产生运行输入。

对当前oa-TOF三套实现的静态拓扑审计进一步关闭了“入口是否已存在”这一问题：COMSOL的`accelshield`
是完整外方块减内方块并带后盖，SIMION加速器PA同样是外方框减内方框再加后封闭，CAD则把这一
`accelshield`直接作为单一`accelerator_shield`实体导出；三者沿RF映射后的oa `+x`注入方向都没有侧孔。
按当前baseline，屏蔽内半宽为`5+5+5=15 mm`、壁厚`4 mm`、加速器轴`x=-48.8 mm`，故负x侧内/外
表面分别为`-63.8/-67.8 mm`。合同将外表面上`(-67.8, 0, -18.42918680341103) mm`登记为被实体材料
阻塞的入口参考面，而不是已有开口。连接器可以承担相对位移、接地注入管和转向/聚焦，但必须同时
驱动oa屏蔽的参数化开孔候选；只在屏蔽外添加连接器仍会撞壁。孔径、管长和场泄漏容差尚未选择，
因此拓扑审计为FAIL且物理运行继续禁止。

入口孔现改为“先理论、后候选”的失败关闭合同，不允许用透过率扫描反向挑孔径。当前oa正式baseline
给出一级加速场`E_A1=(2240-1760)/3=160 V/mm`；由固定二级反射区长度、电压和100%穿透深度裕量
反解得到允许最高能量每电荷`2080 V`，故空间相关轴向完整宽度上限为
`2×(2080-2000)/160=1.0 mm`，即半高`0.5 mm`。这比首加速间隙给出的零安全余量几何半高
`1.5 mm`更严格，因而是当前已知控制约束；两者不得混用完整宽度与半高。最终物理孔还必须小于接地
注入管场泄漏、经粒子性能标定的三维场扰动、高压/击穿和机械间距等上限，并乘显式安全系数；传播到
入口的束流包络不是默认硬几何下限，超出孔径的部分可以作为明确报告的几何损失，
束流包络、装配偏差和传输裕量下限。上述泄漏容差、有效管长、安全间距、束流分位和安全系数尚未
冻结，因此合同仍不填孔形或尺寸，也不生成COMSOL/SIMION/CAD候选。

防回归由`../analysis/entry_aperture_l0.py`和Static门禁承担：1 mm不作为可独立修改的经验常数，而从
oa baseline的电极位置、电压、反射区长度和裕量策略每次重算；合同中的完整宽度/半高语义和派生值
必须同时吻合。特征化候选可按显式安全系数直接取`设计完整高度 ≤ 安全系数×min(全部理论上限)`，同时
必须严格小于未乘安全系数的理论上限；本轮`0.9 mm=0.9×1.0 mm`满足该语义。完整束流支撑不是默认硬
下限；未冻结最低通过率时只允许报告损失，不能得到S1 PASS。进入验收或晋升前，才由整机损失预算冻结
最低通过率并对每一份源束执行同一门禁；缺必需约束、未报告损失或手工重复常数均失败关闭。

所有入口硬约束统一采用逻辑AND。对同一标量尺寸，上限取全部适用上限的最小值，下限取全部适用下限
的最大值；经性能标定且同时依赖横向宽度和轴向高度的约束必须保留为二维可行域，不能擅自拆成两个独立
标量。机器输出必须列出每项裕量、控制约束及未解析项；只有全部硬约束计算并通过后才允许S1 PASS。

## 产物与边界

- 迁移前的COMSOL/SIMION候选、闭合证据、轨迹诊断、独立场闭合和图表统一冻结在
  `artifacts/projects/rf_quadrupole_collision_cooling/archive/20260719_212436__migration-snapshot__repo__pre-v2-layout/legacy-layout/`；
  其中保留原`models/`、`results/`和`runs/`相对结构，`archive_manifest.json`给出入口。
- 新COMSOL、SIMION与cross-solver运行分别进入独立`runs/<run_id>/`；跨求解器运行的run config
  必须引用两个来源manifest，不能再从顶层共享results按文件名猜来源。
  两场只比较、不互相导入；先前 150 mm 简化直杆 MPH 已删除，历史 `test3` 碰撞模型也不能证明本结论。
- 相位--轨迹诊断和分辨率/内部释放旧证据同样位于上述迁移快照；未来输出写入来源run的`results/`。
- 三段式门禁的旧结果与运行记录均位于上述迁移快照的原`results/integration_readiness/`和
  `runs/integration_readiness/`。运行本身success，门禁结论FAIL，两者不得混淆。
- 本阶段未选定机械正式几何，因此没有触发 SolidWorks 同步；若以后提升为正式组件，必须同任务完成 CAD/装配同步。

## 文档与共用边界

本项目沿用OA-TOF的三层结构：项目`README.md`只导航和列项目硬规则，`PROJECT.md`保存当前参数、
跨软件结论和开放任务，`COMSOL.md`/`SIMION.md`只记录各自实现与复验方法。OA-TOF这一层级是合理的，
但其`PROJECT.md`已偏长；RF不继续复制逐轮日志式写法，失效过程只在确有追溯价值时进入`docs/history/`。

SSOT、manifest、独立场比较、相同粒子表和共同物理事件比较已经由根`README.md`与
`docs/VALIDATION_METHODS.md`定义，不在本项目重复建立第二份规则。`particle_state.csv`的通用列语义和
manifest写入/复验工具适合留在`common/contracts/`；RF特有的85.4/90.2/95.2 mm平面、终止分类、
接口阈值及工况仍留在本项目`config/`。待OA-TOF实际采用同一事件表后，再把经两个项目验证的通用
CSV结构校验器上提；现在提前搬迁会把RF假设伪装成跨项目标准。
