# oa-TOF 当前项目状态

本文件是oa-TOF项目的人类可读权威源。机器参数以
[`../config/baseline.json`](../config/baseline.json)为准；COMSOL、SIMION和CAD文档只能记录
各自实现，不能在本文件之外另行定义项目参数。

## 当前统一定义

- 方案：正交加速飞行时间质谱，双级环栈反射镜，一级10环、二级5环。
- 标准离子：524 amu、+1电荷。
- 初始能量：均值5 eV、标准差0.4 eV，负能量重采样。
- 释放体：`1×1×1 mm³`，当前轴向范围`z=1...2 mm`。
- 分辨率：`R=m/FWHM_m`。窄峰时间域等价式为`R=T/(2*FWHM_t)`。
- `2.3548×sigma`只在高斯假设成立时作为FWHM代理；非高斯峰必须直接测量半高宽。
- SIMION常规统计N=5000；跨求解器严格比较优先使用同一固定ION粒子表。
- SIMION正式轨迹积分质量为8，由机器契约、同名Program加载段和IOB运行时门禁共同保证。
- SIMION稳定入口已冻结为0.05mm正式可移植交付与0.025mm轴向收敛参考；实现资产与SHA-256见
  `config/simion_stable_entry.json`，它不替代`baseline.json`的物理契约。

### 参数派生硬规则

本项目严格执行：`物理输入 → 公式 → baseline工程参数 → COMSOL/SIMION/CAD`。机器权威源是
`config/baseline.json`；任何软件现有文件、网格坐标、格式化结果或GUI显示均无权反向修改它。

反射器长度由`U0=2000 eV`、总无场长度`L=1200 mm`、一级长度`d1=120 mm`和二级余量系数
`1.0`按baseline中记录的公式派生。原始计算为`d2=86.8328157299974 mm`、
`L_refl=206.832815729997 mm`；工程长度统一保留小数点后4位，因此baseline值只能是
`d2=86.8328 mm`、`L_refl=206.8328 mm`。COMSOL曾因`%g`序列化得到`206.833 mm`，这是实现
舍入错误，不是新权威值；该错误转正已撤销并归档。长期门禁必须重新计算上述公式，同时检查
COMSOL写入精度、SIMION实体/Program以及CAD均服从同一工程值。

## 当前几何状态

正式紧凑加速器保持轴向排布不变：孔半宽5 mm、环单边宽5 mm、带电电极到接地屏蔽
每侧5 mm、repeller后表面到接地后盖前表面5 mm、屏蔽壁厚4 mm；物理范围约
`38×38×30 mm³`。SIMION日常网格为`xy=0.25 mm、z=0.05 mm`，收敛参考为
`xy=0.25 mm、z=0.025 mm`。

外部屏蔽罩权威定义为COMSOL源码中的一体式封闭圆柱壳：内半径350 mm，侧壁及两端盖厚度
均为10 mm；近端真空面/外表面为`z=-30/-40 mm`，远端真空面/外表面按baseline派生为
`z=881.6628/891.6628 mm`。SIMION以无场区PA提供近端盖和连续侧壁，以反射区PA接续侧壁并
提供远端全截面端盖；Program只负责重叠PA区域的实例路由，不能改变上述几何。加速器出口栅网
是另一个独立的`30×30 mm`、`z=19.83 mm`理想透明栅网，不得误认成大屏蔽罩端盖。

2026-07-16已从修正后的源码重新生成524 amu、固定N=100、真实场、0.2 ns候选；100/100命中，
平均TOF为`71.9868802959 us`；统一Python直接质量FWHM为`0.017910731136 Da`，`R=29256.2038`。候选经
COMSOL参数、封闭屏蔽罩、出口栅网、Study/Solver及关键场值门禁后转为正式MPH。同一任务已用
SolidWorks 2022（revision 30.5.0）重建25个SLDPRT和25组件SLDASM，最大组件世界中心误差
`5.68e-13 mm`，保存错误/警告均为0。因此COMSOL与SolidWorks正式几何状态均为`synchronized`。
先前`206.833 mm`错误候选及旧CAD正式目录仅保留在archive追溯，不得恢复为权威入口。

SIMION检测器第4实例是半径40 mm、有效面`z=19.83 mm`的GUI可见数值终止层，不是机械
检测器实体。当前网格为`dx/dy/dz=0.5/0.5/0.01 mm`，终止层厚0.05 mm，前/背真空余量
0.2/0.05 mm。COMSOL/CAD机械检测器厚度仍按独立机械契约管理。

## 已闭合的SIMION结论

- 524 amu GUI Fly2、N=5000：5000/5000命中。
- 平均TOF约`71.9902651204 us`。
- 直接时间FWHM约`1.392926 ns`，对应`R=25841.38`。
- 直接质量FWHM约`0.02027719 amu`，对应`R=25841.84`。
- 时间域和质量域直接FWHM结果只差约0.0018%。
- 原生splat与`z=19.83 mm`参考面的最大逐粒子时间差约`4.0e-7 ns`。
- 峰为单个非高斯右肩峰；主要来源是初始z到TOF的二次映射，不是漏检、横向截断或绘图伪影。
- 2026-07-16原生GUI Recording已用Program On、Event=4、PA实例4和固定检测面完成5000粒子严格
  审计；Program Off配对诊断证明关闭Program会停用透明栅网跨越并破坏TOF映射，不能作为基线。

## 2026-07-16正式几何同源N=100数值闭合

COMSOL与SIMION均使用同一份524 amu固定ION表、正式几何和统一Python直接KDE FWHM。正式
COMSOL日常档为真实加速器`hmax=1 mm`、敏感窗口`0.2 ns`、无场区`50 ns`；100/100命中，
`FWHM_m=0.017041315788 Da`、`R=30748.7994`。SIMION quality=8为100/100命中，
`FWHM_m=0.019673808666 Da`、`R=26634.3954`。COMSOL点估计高`15.45%`，平均TOF仅短
`3.0443 ns`。标准化KDE重叠为`0.8545`，KS距离`0.14`且`p=0.2819`，总体分布仍接近。

峰结构仍未完全一致：COMSOL保持2个显著KDE模态，SIMION为单模态；COMSOL右肩更明显。
5000次配对bootstrap的绝对R差异2.5%/中位数/97.5%分位为
`0.8634%/18.8517%/95.8774%`，区间很宽，说明N=100不能精确量化R差异。正式结论同时报告下述
0.5 mm收敛档，不把日常档单一R当作跨求解器真值。

### 2026-07-16同源N=1000正式统计闭合

为解决N=100无法精确判断R差异的问题，COMSOL与SIMION已使用同一份固定N=1000 ION表重算。
该表前100行与原正式N=100表逐行相同。SIMION前100粒子TOF与旧结果完全一致；COMSOL前100粒子
相对旧N=100结果的最大TOF差仅`1.92e-8 ns`，证明扩样没有改变轨迹积分结果。COMSOL复用正式MPH
已存静电场，只重算GUI可见的1 mm、0.2/50 ns粒子研究，耗时`714.95 s`；两边均1000/1000命中。

统一直接KDE FWHM得到COMSOL `FWHM_m=0.01359256443 Da, R=38550.49`，SIMION
`FWHM_m=0.01874021531 Da, R=27961.26`，COMSOL点估计高`37.87%`；平均TOF仍只短
`3.1801 ns`。标准化KDE重叠为`0.8002`，KS距离`0.115`、`p=3.53e-6`。N=1000时两边都出现
2个显著KDE模态，但COMSOL主峰更窄，SIMION右肩更强，因此差异是峰结构差异而不是整体飞行时间偏移。

5000次配对bootstrap的绝对R差异2.5%/中位数/97.5%分位为
`15.604%/37.895%/112.437%`。下限已经排除零，完成了N=100不能完成的确定性判断；上限没有按简单
`1/sqrt(N)`预期收窄。该长尾与多模态峰在重采样时改变全局主峰半高交点的结构敏感性一致，不能
解释为轨迹不复现；增加bootstrap次数只会降低Monte Carlo噪声，不能消除这种FWHM采样分布。
当前N=1000是正式统计闭合；N=100继续承担1/0.5 mm网格和1/50 ns时间输出的成对数值收敛角色。

大样本source mapping继续支持原定位：COMSOL/SIMION时间焦点为`z=1.60546/1.41023 mm`，相差
`0.19523 mm`，z二次模型分别解释`90.63%/89.01%`的TOF方差。检测器落点质心距离`0.0913 mm`，
同粒子落点平均/RMS/最大距离为`0.1410/0.1619/0.5562 mm`，几何与横向传输仍保持接近。

### 峰肩来源诊断

正式N=100同源粒子的z-to-TOF二次映射已经闭合到可解释状态。COMSOL日常档时间焦点顶点在
`z=1.58204 mm`，SIMION在`z=1.40316 mm`，相差`0.17888 mm`；二次曲率分别为
`0.00867780/0.00928120 us/mm²`，说明曲率接近而焦点位置发生平移。z单变量二次模型已分别解释
`93.22%/88.20%`的TOF方差，加入初始能量、x和y后升到`93.39%/88.66%`，所以右肩差异主要是
纵向时间聚焦映射，不是横向截断或能量表导入错误。

相同坐标的场采样进一步定位到加速段：释放区SIMION轴向Ez平均比COMSOL高`0.8396%`；反射器
内部（排除电极边界0.30 mm插值带）相对RMS差仅`0.000528%`。18、52、97号代表粒子在加速器
出口时SIMION比COMSOL晚`0.29--2.11 ns`，到反射器入口扩大为`16.57--22.84 ns`；反射后返回
入口时变为提前`12.64--14.88 ns`，最终检测面只残留`1.94--5.07 ns`。这证明两者的反射区传递
实质闭合，剩余峰肩由小的加速区场差经过时间聚焦放大并再次补偿形成。

### 2026-07-17 SIMION加速器轴向网格相位诊断

为检验非整数尺寸相对`dz=0.05 mm`网格的位置效应，保持全部机械坐标不变，把加速器几何在PA内
平移`0/0.0125/0.025/0.0375 mm`并反向补偿实例位置；四个相位均使用前后`0.2 mm`扩展裁边、同一
固定N=100和独立SIMION场。正式裁边与扩展裁边的平均TOF差仅`4.83e-7 ns`、FWHM差`2.83e-5%`，
排除裁边混淆。相位扫描的全加速段轴线场相对RMS最大仅`0.001718%`，释放区平均Ez跨度仅
`1.15e-7%`，不能解释COMSOL/SIMION现有`0.8396%`释放区平均场差。

粒子传递却对网格相位显著敏感：为可靠越过一格厚的透明数值栅网，全部相位统一使用`0.03 mm`
跳转缓冲时，平均TOF跨度`1.62898 ns`、同粒子TOF RMS差最大`2.46777 ns`；用`0.04 mm`复核时
平均TOF跨度`1.65565 ns`，两次跨度只差`1.64%`，约为当前跨求解器平均TOF差的`51--52%`。
直接FWHM跨度则从`32.89%`变为`19.35%`，说明峰宽仍与透明栅网跨越细节耦合。正式`0.005 mm`
缓冲下，半格相位粒子会在`grid1,z=3 mm`数值层splat；这不是机械传输失败，而是固定跳转距离未
越过该网格相位下的电极层。

结论：`surface=fractional`已使静电场基本不受相位影响，但当前“一网格点电极层+固定距离跳转”会
把网格相位放大为ns级逐粒子TOF差。非整数网格对齐是SIMION高精度粒子映射的重要误差源，但不是
现有平均场差的来源，也不能单独解释全部COMSOL/SIMION差异；不得通过改物理尺寸迁就网格。

2026-07-16已完成加速器全域局部网格收敛。当前正式日常`hmax=1 mm`和高精度
`hmax=0.5 mm`分别为336077和659685个单元；从1 mm加密到0.5 mm时，同坐标Ex/Ey/Ez变化RMS为
`79.47/75.09/80.05 V/m`。0.5 mm相对SIMION的Ex/Ey差异RMS已降到`61.90/42.62 V/m`，证明原
COMSOL离轴锯齿主要是粗四面体网格的电势梯度伪影；剩余Ez差异RMS为`781.70 V/m`，平均偏差
`-612.80 V/m`，并未随横向伪场一起消失。

同一固定N=100的0.5 mm收敛档为100/100命中、`R=28837.88`；正式1 mm日常档为
`R=30748.80`，相差`6.63%`。两档标准化峰形重叠`0.9677`，KS距离`0.07`、`p=0.9684`，均为2个
显著KDE模态；同粒子落点平均/RMS/最大距离为`0.2309/0.2695/0.7115 mm`。0.5 mm档对SIMION
为`R=28837.88/26634.40`，COMSOL高`8.27%`，峰形重叠`0.8815`，质心距离`0.1041 mm`。
因此1 mm正式档用于日常计算，0.5 mm只作为收敛项和正式结论的误差参考。

时间输出也已分层：正式日常档使用参数化相位对齐窗口，敏感区`0.2 ns`、无场区`50 ns`；N=20
相对高精度大窗口基线逐粒子等价并加速`2.89x`，N=100相对`1 ns`分段档逐粒子等价并加速
`1.77x`。质量、电压、无场区长度或反射器参数变化会联动GUI事件时间；实际事件若进入安全裕量，
构建门禁直接失败。高精度大窗口结果不得被日常档覆盖。

### 历史固定粒子候选验证（已被正式闭合取代）

同一份524 amu、N=100固定ION表已在SIMION quality=8与全三维COMSOL真实场候选中比较。COMSOL
`t=0`释放位置逐元素与ION表的最大误差为`7.1e-15 mm`，速度模长最大误差为`4.2e-4 m/s`，故两者
确实使用相同初始条件，不存在坐标列、单位或速度列导入错误。

两边均为100/100命中，平均TOF分别为`71.98684756 us`（COMSOL）和`71.99013526 us`（SIMION），
仅差`3.29 ns`。统一直接质量FWHM/KDE口径下，COMSOL为`0.01760645 Da, R=29761.82`，SIMION为
`0.01967453 Da, R=26633.42`；差约11.75%。时间步与SIMION trajectory quality不是可一一换算的
同一参数，因此不以强行调成同一R作为验收目标。

以下内容只解释2026-07-15候选如何排除了粒子表导入错误，不再表示当前模型仍是候选。峰形比较图
和原始到达时间保存在`artifacts/projects/oa_tof/results/cross_solver/`及
`artifacts/projects/oa_tof/runs/*fixed_particle_closure/2026-07-15/`。两边都是右偏主峰带右肩，
标准化KDE重叠为`0.869`、KS距离`0.140`，可判为主峰结构一致；但不是逐点闭合：COMSOL有约10%
高度的弱次肩、偏度更高，且TOF对初始z的相关性为`-0.691`，SIMION为`+0.373`。这说明剩余差异来自
同初始条件下的场/轨迹传递映射，而不是随机ION表导入。该候选已经由上文2026-07-16正式MPH、
轴线场、代表轨迹和网格收敛结论取代；候选数值不得再作为当前性能基线。

### 2026-07-16求解器无关分析基线

项目已固定Python 3.11参考分析层，机器定义见`config/analysis_contract.json`，冻结数据身份和
回归值见`config/analysis_baselines.json`。正式直接FWHM统一为Gaussian KDE全局主峰两侧的线性
半高交点；带宽固定为`1.06*sigma*N^(-1/5)`，评价网格4001点。`2.3548*sigma`仍只作高斯代理。

四个外部基准的路径、行数、字节数和SHA-256已通过门禁。固定524 amu、N=100比较中，新Python
结果与原MATLAB跨求解器算法近乎相同：COMSOL/SIMION直接质量FWHM分别为
`0.0176061408/0.0196738139 Da`，R分别为`29762.34/26634.39`；标准化KDE重叠`0.86909855`、
KS距离`0.14`、配对相关`0.346157`均复现。

旧SIMION单独分析使用另一带宽`0.9*min(sigma,IQR/1.34)*N^(-1/5)`。因此同一N=5000数据迁移到
统一带宽后，524 amu直接质量FWHM由`0.02050914`变为`0.02148581 Da`，100 amu由
`0.00376789`变为`0.00408007 Da`；对应R变化只表示分析定义变化，不是轨迹或网格变化。旧数值
继续保留为迁移参考，不得与新值无标注混用。参考产物位于
`artifacts/projects/oa_tof/results/reference_analysis/baseline/`。

纯后处理迁移已闭合：Python现在直接读取SIMION`detector_crossing` TRACE，严格审计GUI Recording
的Event/PA instance/固定z面/有效半径，自动分析初始x/y/z/能量到TOF映射，并负责配对bootstrap。
原来的三个SIMION MATLAB分析器和跨求解器MATLAB峰形脚本已删除；COMSOL MPH脚本缩减并改名为
`export_fixed_particle_arrivals_from_mph.m`，只保留MPH读取、释放核对、检测面插值和CSV导出。

固定N=100、5000次配对bootstrap（seed 20260715）在统一直接KDE FWHM口径下给出的绝对R差异
2.5%/中位数/97.5%分位为`0.6273%/16.2962%/90.4652%`。旧MATLAB口径相应为
`0.7298%/17.1724%/93.0698%`，结构一致；区间很宽说明N=100不足以精确量化R差异，不能据此把
11.75%的观测差强判为确定的场差异。峰形、平均TOF和z-to-TOF映射证据仍用于定位确定性传递差异。

### 2026-07-17 加速器 0.1 mm 尺度严格聚焦候选

按三栅加速器解析式建立了独立候选：保持`d1=3.0 mm`、电压和五环对称等间距不变，把
`d2`从`16.83 mm`改为`16.8 mm`，并将加速器与粒子源整体平移
`-0.098642137223724 mm`。新grid2为`z=19.7013578627763 mm`，一阶焦点严格保持在原理论值
`z=19.8305446661873 mm`；工程检测面仍为`19.83 mm`，其`0.5447 um`偏差的一阶等效展宽仅约
`0.55 ps`，相对当前ns级峰宽可忽略。机器契约位于
`config/candidates/accelerator_grid_aligned_strict_focus.json`。

COMSOL和SIMION分别使用自己的静电解与同源平移N=100粒子表，均100/100命中。COMSOL/SIMION
平均TOF为`71.9901770195/71.9932152462 us`，相差`3.03823 ns`，与正式几何的约`3.0443 ns`
实质相同；直接质量R为`31611.76/26146.18`，N=100峰形仍分别为双模/单模，不能据此声称精度
改善。同一27个场采样点上，正式几何与候选的Ez差异RMS分别为`719.03/727.75 V/m`
（相对COMSOL RMS为`0.6036%/0.6103%`）；Ex差异RMS由`46.28`增至`64.58 V/m`，Ey由
`46.69`增至`47.44 V/m`。因此候选证明了严格焦点与下游时序可保持，但没有证明跨求解器场闭合
或粒子精度提高。它继续保持候选，`baseline`、正式MPH、SIMION正式包和SolidWorks装配均未改变。

### 2026-07-17 透明栅网跳转距离候选

在不改变任何PA场的前提下，对严格聚焦候选扫描`ideal_grid_epsilon_mm=0.0001...0.04 mm`。
跳转越接近理想零厚度面，SIMION与COMSOL逐粒子TOF越接近；N=100的`0.005 -> 0.0001 mm`
使平均差从`3.0382`降至`0.7225 ns`，逐粒子RMS从`3.1521`降至`1.0012 ns`，最大差从
`5.1065`降至`2.3088 ns`。`0.0001/0.00025/0.0005 mm`均100/100命中，结果已进入小距离
收敛区，不存在因跳转过小重新splat的证据。

分组析因表明主要收益来自已对齐网格的反射器`entgrid/midgrid`：只缩小加速器两栅时平均差为
`2.7191 ns`，只缩小反射器两栅时为`1.0087 ns`，两组同时缩小时为`0.7225 ns`。进一步在不改
正式几何的副本上只设反射器为`0.0001 mm`，N=1000相对冻结COMSOL的平均/逐粒子RMS/最大TOF差
由`3.1801/3.3119/5.7704 ns`改善为`1.1508/1.3986/3.0616 ns`，配对相关由`0.3091`升至
`0.4290`，焦点差由`0.1952`降至`0.1736 mm`。但标准化KDE重叠由`0.8002`降至`0.7564`，不能
只因SIMION直接R从`27961`变为`39740`、接近COMSOL的`38550`就转正。一次同环境N=1000计时为
小跳转`35.37 s`、原跳转`38.44 s`，没有计算变慢迹象，但单次计时不作为性能基准。

程序已增加加速器/反射器独立覆盖参数，默认均为`-1`并回退到正式`0.005 mm`，所以当前正式行为
不变。该数值改进继续保持候选；转正前需冻结选择值、复核GUI Data Recording、重建正式交付包并
更新稳定入口哈希，不能用“R更接近”代替完整峰形验收。

## 正式化门禁

候选几何只有同时满足以下条件才能转正：

1. COMSOL与SIMION共享几何契约通过。
2. 使用同源粒子比较命中率、平均TOF、直接质量FWHM、分辨率和峰形。
3. 差异有网格收敛或明确物理原因支持。
4. 正式COMSOL MPH已更新并能在GUI中重算。
5. SolidWorks零件和装配体在同一任务更新，保存错误/警告和坐标检查均通过。

## 下一步

1. N=1000统计扩样已闭合；除非物理参数、几何或FWHM定义改变，不继续单纯增加N或bootstrap次数来
   压缩多模态直接FWHM的长尾上限，也不据此调整场参数追平单一R。
2. COMSOL加速器网格收敛已闭合；不继续为追平单一R而调网格、时间步或SIMION quality。若将来更新
   正式MPH，局部Size必须位于Free Tetrahedral之前并重新做GUI Compute。
3. **未来工作：SIMION透明栅网去网格相位化。** 在追求跨求解器亚ns逐粒子闭合前，使grid1/grid2的
   粒子跨越自动越过实际数值电极层，并证明结果对网格相位和合理跳转缓冲收敛；不得改baseline尺寸
   使其凑成网格整数倍。当前正式`0.05 mm/0.005 mm`包继续承担已冻结的统计复现，不提升为高精度基线。
4. 不依赖原工作区路径的SIMION同事复现包已完成；二维轴对称COMSOL混合模型仍是未来工作。
5. **未来工作：SIMION正式包从源重建与发布。** 当前正式包已可独立复制、加载和飞行，暂不重构；
   后续使四套完整PA家族直接由版本化baseline、GEM和构建器在scratch中从头生成，不再读取旧
   `workspace/diagnostics` PA或模板。候选通过完整哈希、四实例、quality、关键场和固定粒子飞行门禁后
   再提升到formal；发送时按需生成不含收敛参考、日志和临时轨迹的ZIP及独立SHA-256，发送后ZIP可删除。
6. **未来工作：制造与装配误差预算。** 以参数化几何为唯一来源，研究电极
   尺寸/同轴度/间隙/倾斜、绝缘件厚度、反射镜环位置和电压分配误差、检测面位置，以及材料热膨胀
   和装配基准误差对命中率、峰形和直接`FWHM_m`的影响；先做局部灵敏度，再做受公差约束的组合
   Monte Carlo，输出分辨率良率与制造公差建议。该项不得用当前候选的数值误差冒充制造误差。

## 2026-07-15 目录迁移验证

四个项目的大型资产已整体迁到`artifacts/projects/`。当前0.05 mm z网格候选IOB在新路径下由
SIMION无GUI实际加载成功，四个实例分别解析为849×356×1反射器、153×153×601加速器、
601×355×1无场管和165×165×31检测器；网格与预期一致。因此顶层迁移没有破坏相对PA引用。
SIMION内部`workspace/diagnostics`原先较深，不能用文件系统扁平移动代替IOB重建。2026-07-17已将
正式0.05 mm实现集中到`models/simion/formal/oatof_524amu/`，四套完整PA家族、IOB/CON、同名
Program/Fly2、N=100/N=1000固定ION、复现说明和SHA256清单位于同一目录。新IOB实际加载四实例、
quality=8与关键场值均PASS；固定N=100为100/100命中，平均TOF `71.990135072569 us`，与原正式
结果一致。旧`workspace`仅保留收敛参考、构建源和待审计历史，不再是正式运行依赖。
