# Single-flight 硬限制审计

审计日期：2026-08-31。范围是 `runtime/run_single_flight.ps1` 的生产失败关闭；目标不是增加门禁，
而是区分不可继续的证据/物理错误与可以结构化处理的正常状态。

| 保留的失败关闭类别 | 典型位置 | 防止的具体故障 |
|---|---|---|
| 冻结身份与来源闭合 | 75–78、131–138、294–311、376–478、812–1031、2990–2997 | 将不同 candidate、layout、field、source、clock、cohort 或损坏 cache generation 混入同一结果；或路径逃逸后复用错误产物。 |
| 模式与阶段边界 | 233、646–724、789–800、2246–2266、3033–3071、3200–3207、3305–3315 | 将 axis-only 误作粒子飞行，把 continuation 用于不支持的模式，或细域/overlay/IOB 缺失时静默落入零场/重复飞行。 |
| 物理、数值与拓扑合法性 | 201–205、855–868、1162–1187、1244–1251、1444–1447、2351–2376、2557–2566、2745–2917、3282、3400–3405、3621–3625 | 使用负 gap、非有限网格、冻结网格不一致 PA、非连续 basis、错误孔拓扑、零场筛选状态，或产生漏粒子/错误重启容差的数值结果。 |
| 求解器、PA 与证据构建 | 1283、1337–1374、1452–1458、1525–1561、1611–1628、1714–1766、1803–1857、1961–2029、2433–2526、2605–2725、2766–2911、3210–3226、3554–3561 | GEM、默认 refine、Dirichlet basis、接口验收失败后仍把错误 PA 当真实场；或 `require_existing` 下静默重建，改变冻结实验。 |
| 资源与容量 | 560–634、1357–1364、1754–1765、2618–2719、3243–3248、3463–3496、3554–3558、3588–3591、3725–3727 | 低于容量水位、内存危险、超冻结 run/cache 预算时把不完整或不可保留的证据标为成功。 |

以下已是适当的结构化 warning：机械孔与网格不对齐（1251–1255）、大 ion list 的可视性提示
（3411–3415）、终态别名清理失败（1400–1403、3740、3761）以及终态后的容量复核失败
（3797–3806；科学 manifest 已不可变，不能由清理失败重写）。

唯一应改为正常决策而非异常的候选是 3308：所有 pre-pulse batch 已完成时，返回
`already_completed/use_recovery` 并转入零 SIMION recovery；不得把它解释为可再飞一次。实施时需增加
“完成 continuation 只生成 recovery instruction、不会启动 SIMION”的测试。

`TheoryWorkingPoint` 是可选输入；在没有获批的误差阈值前，只能导出并报告实际轴场，不能因缺失自动失败或
自动放行。短 gap 的 50 mm/10 mm 条件只是 profile 路由，不是运行时不可用限制。
